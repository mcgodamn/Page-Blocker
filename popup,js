function applyChange()
	{
		var hard = document.getElmentById("hard").checked;
		chrome.storage.sync.set({
    			blockway: hard,
  			}, function() {
  				if (hard) {
  					message('u want hard way');
  				}
    			else 
    			{
    				message('u want soft way');
    			}
 			});
	}